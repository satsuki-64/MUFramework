# MUFramework
MUFramework is a lightweight code framework built for Unity, implementing an asset system, an MVVM framework, a logging system, common design patterns, character/camera control, and commonly used shaders. The shaders are based on URP.
